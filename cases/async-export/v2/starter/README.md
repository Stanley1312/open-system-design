# Async Export V2 — Starter

This scaffold keeps the HTTP and worker boundaries visible while leaving the
queue implementation to you. Start with the requirements in ../CHALLENGE.md.

## Run

    uv sync
    uv run python main.py

The unfinished endpoints return 501 Not Implemented until you complete the
TODOs in main.py, db.py, and worker.py.
